%h1 Your Workers

%table
  %tr
    %th Username
    %th Password
  - @workers.each do |worker|
    %tr
      %td= worker.username
      %td= worker.password

= button_to "Create new worker...", new_worker_path, :method => :get